var modalMap = $("#modalMap");
var modal = $("#myModal");
var content = $('.modal-content');
var title ="Oiseaux";


//draggable

content.draggable( ); //launch one to initialize
$(".modal-header").hover( function(){
            content.draggable( "option", "disabled", false);
        },
        function(){
            content.draggable( "option", "disabled", true );
        });

var launchModal = function(title){
    console.log("modal");
    //set title
    $(".modal-header h2").text( title );
    modal.css({display : "block" });
    var $close = $(".close");
    $close.on("click", function() {
        modal.css({display : "none" });
    });
    // When the user clicks anywhere outside of the modal, close it
    window.onclick = function(event) {
        if (event.target == modal) {
            modal.css({display : "none" });
        }
    };
    $(".modal-header h2").text( title );
    modal.css({display : "block" });
}
// When the user clicks anywhere outside of the modal, close it
window.onclick = function(event) {
    if (event.target == modal) {
        modal.css({display : "none" }
        );
    }

};